# 只启用安全规则集：
#   F  = pyflakes（未使用导入 F401、未定义名 F821、未使用局部变量 F841 等）
#   E9 = 语法错误
# 不开 UP/W/I 等风格规则：本地测试跑 Python 3.9，UP007 的 `X | Y` 建议一旦
# 被 --fix 应用会破坏 3.9 兼容。需要风格化请单独显式运行，不要改这里。
[lint]
select = ["F", "E9"]
