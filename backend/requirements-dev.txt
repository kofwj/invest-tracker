# 测试/开发依赖 — 不进生产镜像。
# 本地跑测试：pip install -r backend/requirements-dev.txt
-r requirements.txt
# 这两个按本仓库开发环境（macOS + Python 3.9）实际在跑、且 tests 全绿的版本钉死。
# 生产容器里不装 dev 依赖，所以没有容器 freeze 可参照；如需升级请先跑全量 pytest 再改。
pytest==8.4.2
httpx==0.28.1
