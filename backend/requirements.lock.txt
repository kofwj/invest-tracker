# 生产依赖「完整快照」（直接 + 传递），取自生产容器 `pip freeze`，目标解释器是
# 镜像里的 Python 3.11-slim。
#
# 用途：需要逐字节可复现的环境（审计、迁移、排查"同一个持仓为什么算出不同数字"）：
#     pip install -r backend/requirements.lock.txt
# 代替 requirements.txt 安装。
#
# 边界说明：
#   - Dockerfile 仍然只装 requirements.txt（直接依赖已 == 锁死），本文件不参与默认
#     构建：把传递依赖也钉死后，升级 akshare / pandas 要同步改两处，很痛，所以默认不用。
#   - 本地 Python 3.9 开发环境请继续用 requirements-dev.txt（本文件的传递依赖按
#     Python 3.11 解析，未必都能装在 3.9 上）。
#   - 改完 requirements.txt 后，重新在容器里 `pip freeze` 覆盖本文件。
-r requirements.txt
akshare==1.18.97
# 注：akracer / annotated-doc 是 akshare 1.18.97 自己声明的依赖，未出现在交付给我的
# 容器 freeze 清单里；这里按镜像上真实解析/安装出来的版本一并钉住（已实测可装）。
akracer==0.0.14
annotated-doc==0.0.5
annotated-types==0.8.0
anyio==4.15.1
beautifulsoup4==4.15.0
certifi==2026.7.22
cffi==2.1.1
charset-normalizer==3.5.1
click==8.5.0
curl_cffi==0.16.3
decorator==5.3.1
et_xmlfile==2.0.0
fastapi==0.141.1
h11==0.16.0
html5lib==1.1
idna==3.20
jsonpath==0.82.2
lxml==6.1.3
numpy==2.4.6
openpyxl==3.1.5
packaging==26.3
pandas==2.3.3
py-mini-racer==0.6.0
pycparser==3.0
pydantic==2.13.5
pydantic_core==2.46.5
python-dateutil==2.9.0.post0
python-multipart==0.0.32
pytz==2026.3.post1
requests==2.34.2
six==1.17.0
soupsieve==2.9.2
starlette==1.6.0
tabulate==0.10.0
tqdm==4.70.1
typing-inspection==0.4.4
typing_extensions==4.16.0
tzdata==2026.4
urllib3==2.8.0
uvicorn==0.53.0
webencodings==0.6.1
xlrd==2.0.2
